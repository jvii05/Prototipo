@model CASEPro.Models.CASO_USO_DETALLES

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>CASO_USO_DETALLES</legend>

    <div class="display-label">CASO_USO_ID</div>
    <div class="display-field">@String.Format("{0:F}", Model.CASO_USO_ID)</div>

    <div class="display-label">DETALLE_TIPO_ID</div>
    <div class="display-field">@String.Format("{0:F}", Model.DETALLE_TIPO_ID)</div>

    <div class="display-label">CONTENIDO</div>
    <div class="display-field">@Model.CONTENIDO</div>

    <div class="display-label">CASOS_USOS</div>
    <div class="display-field">@Html.DisplayTextFor(_ => Model.CASOS_USOS).ToString()</div>

    <div class="display-label">DETALLE_TIPOS</div>
    <div class="display-field">@Html.DisplayTextFor(_ => Model.DETALLE_TIPOS).ToString()</div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>


