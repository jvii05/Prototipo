@model CASEPro.Models.REQUERIMIENTO_DETALLES

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>REQUERIMIENTO_DETALLES</legend>

    <div class="display-label">REQUERIMIENTO_ID</div>
    <div class="display-field">@String.Format("{0:F}", Model.REQUERIMIENTO_ID)</div>

    <div class="display-label">DETALLE_TIPO_ID</div>
    <div class="display-field">@String.Format("{0:F}", Model.DETALLE_TIPO_ID)</div>

    <div class="display-label">CONTENIDO</div>
    <div class="display-field">@Model.CONTENIDO</div>

    <div class="display-label">DETALLE_TIPOS</div>
    <div class="display-field">@Html.DisplayTextFor(_ => Model.DETALLE_TIPOS).ToString()</div>

    <div class="display-label">REQUERIMIENTO</div>
    <div class="display-field">@Html.DisplayTextFor(_ => Model.REQUERIMIENTO).ToString()</div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>


